{
  "compilerOptions": {
    "jsx": "react-jsx",
    
    /* Language and Environment */
    "target": "es2016",                         /* Set the JavaScript language version for emitted JavaScript. You can set this to es6, es5, etc. */
    "module": "commonjs",                       /* Specify the module system (commonjs for Node.js, esnext for ES6 modules). */
    "esModuleInterop": true,                    /* Enable compatibility between CommonJS and ESModules. */
    "forceConsistentCasingInFileNames": true,   /* Ensure that file names are case-sensitive. */
    "strict": true,                             /* Enable all strict type-checking options. */
    "skipLibCheck": true,                       /* Skip type checking of declaration files for faster compilation. */
                             /* Set JSX compilation to support React. */

    /* Emit */
    "outDir": "./dist",                         /* Set the output directory for compiled JavaScript files. */
    "rootDir": "./src",                         /* Set the root directory for your source files. */
    "sourceMap": true,                          /* Generate source maps for debugging. */

    /* Module Resolution */
    "moduleResolution": "node",                 /* Use Node.js module resolution strategy. */
    "resolveJsonModule": true                    /* Allow importing JSON files as modules. */
  },
  "include": [
    "src/**/*"  ,"node_modules/@microsoft/signalr"                                /* Include all files inside the src folder for compilation. */
  ],
  "exclude": [
    "node_modules"                              /* Exclude the node_modules folder from compilation. */
  ]
}
